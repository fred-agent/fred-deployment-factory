# Copy this file to `fredlab-secrets.values.yaml` and fill real values locally.
# `fredlab-secrets.values.yaml` is intentionally ignored by Git.

postgresql:
  admin:
    password: ""
  keycloak:
    password: ""

keycloak:
  admin:
    password: ""
